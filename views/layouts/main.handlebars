<!DOCTYPE html>
<html lang="pt-br" class="h-screen">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/output.css" rel="stylesheet">
    <title>Main</title>
</head>
<body class="min-h-screen h-screen w-screen overflow-x-hidden flex flex-col">




    {{>navbar}}
    {{#if error}}
        <div>{{error}}</div>
    
    {{else}}

    {{/if}}
    {{>msg}}
        
    <main class="flex-grow flex ">
        {{{body}}}
    </main>
    {{#if user}}
        <footer class="shrink-0">
            {{>footer}}
        </footer>
    {{/if}}
</body>

</html>