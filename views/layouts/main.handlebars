<!DOCTYPE html>
<html lang="pt-br" class="h-screen">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/output.css" rel="stylesheet">
    <title>Main</title>
</head>
<body class=" h-screen w-screen overflow-y-hidden overflow-x-hidden flex flex-col">




    {{>navbar}}
    {{#if error}}
        <div>{{error}}</div>
    
    {{else}}

    {{/if}}
    {{>msg}}
        
    <main class="flex-grow">
        {{{body}}}
    </main>
    {{#if user}}

        {{>footer}}
    {{/if}}

</body>
</html>